{
  "name": "accessibility-widget-for-websites",
  "version": "0.1.0",
  "private": true,
  "description": "Free, self-hosted accessibility toolbar widget for any website.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
